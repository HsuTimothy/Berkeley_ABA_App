# Uncomment this line to define a global platform for your project
platform :ios, '9.0'

target 'Berkeley ABA App' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Berkeley ABA App

  target 'Berkeley ABA AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Berkeley ABA AppUITests' do
    inherit! :search_paths
    # Pods for testing
  end
def shared_pods
    pod 'SDWebImage', '~>3.8'
    pod 'Firebase', '>= 2.5.0'

end
